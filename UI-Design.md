# AInnoStar UI 设计规范

> AInnoStar 产品全生命周期管理平台视觉与交互规范，基于 Glassmorphism 设计语言。

---

## 1. 技术栈

| 层级 | 技术选型 |
|------|---------|
| 框架 | React 18 + TypeScript |
| 构建工具 | Vite 6 |
| CSS 方案 | TailwindCSS 3.4 + CSS Variables |
| 图标库 | Lucide React |
| 路由 | React Router v6 |
| 图表 | Recharts / @xyflow/react（流程图） |
| Markdown | react-markdown + remark-gfm |
| 动画 | TailwindCSS animation + CSS transition |

---

## 2. 设计语言概述

采用 **Glassmorphism（毛玻璃）+ 柔和渐变** 的现代设计语言，营造通透、专业、科技感的 B 端平台氛围。

核心视觉特征：
- **半透明面板**：`backdrop-filter: blur(18px)` + 低透明度渐变背景
- **柔和阴影**：大半径散射阴影，避免硬边
- **圆角体系**：统一使用大圆角（`rounded-xl` / `rounded-2xl` / `rounded-3xl`）
- **深浅双主题**：完整支持 Light / Dark 模式
- **微动效**：页面加载淡入、卡片滑入、状态脉冲

---

## 3. 色彩体系

### 3.1 CSS 变量定义

```css
/* Light Mode */
:root {
  --ui-surface: #f5f7fb;
  --ui-panel: rgba(255, 255, 255, 0.88);
  --ui-panel-strong: #ffffff;
  --ui-border: rgba(148, 163, 184, 0.22);
  --ui-text: #0f172a;
  --ui-muted: #64748b;
  --ui-accent: #4f6bff;
  --ui-accent-soft: rgba(79, 107, 255, 0.12);
  --ui-sidebar: linear-gradient(180deg, #fcfdff 0%, #f4f7ff 100%);
}

/* Dark Mode */
html.dark {
  --ui-surface: #07111f;
  --ui-panel: rgba(12, 24, 42, 0.58);
  --ui-panel-strong: rgba(14, 28, 48, 0.72);
  --ui-border: rgba(125, 166, 255, 0.14);
  --ui-text: #e8f1ff;
  --ui-muted: #9db1cf;
  --ui-sidebar: linear-gradient(180deg, rgba(6, 14, 28, 0.92) 0%, rgba(10, 22, 40, 0.88) 100%);
}
```

### 3.2 主色调

| Token | Light | Dark | 用途 |
|-------|-------|------|------|
| Primary | `#4f6bff` | `#60a5fa` | 主按钮、链接、关键操作 |
| Primary Soft | `rgba(79,107,255,0.12)` | `rgba(37,99,235,0.22)` | 选中态背景、标签背景 |
| Surface | `#f5f7fb` | `#07111f` | 页面底色 |
| Panel | `rgba(255,255,255,0.88)` | `rgba(12,24,42,0.58)` | 卡片、面板 |
| Border | `rgba(148,163,184,0.22)` | `rgba(125,166,255,0.14)` | 边框、分隔线 |
| Text | `#0f172a` | `#e8f1ff` | 正文文字 |
| Muted | `#64748b` | `#9db1cf` | 辅助文字 |

### 3.3 TailwindCSS 扩展色板

```js
// tailwind.config.js
colors: {
  primary: {
    50: '#f0f4ff', 100: '#dbe4ff', 200: '#bac8ff', 300: '#91a7ff',
    400: '#748ffc', 500: '#5c7cfa', 600: '#4c6ef5', 700: '#4263eb',
    800: '#3b5bdb', 900: '#364fc7',
  },
}
```

### 3.4 语义色彩

| 语义 | Light | Dark | Tailwind 对应 |
|------|-------|------|--------------|
| Success | `#047857` bg `rgba(16,185,129,0.12)` | `#a7f3d0` bg `rgba(16,185,129,0.18)` | emerald |
| Warning | `#b45309` bg `rgba(245,158,11,0.12)` | `#fde68a` bg `rgba(245,158,11,0.18)` | amber |
| Danger | `#b91c1c` bg `rgba(239,68,68,0.12)` | `#fecaca` bg `rgba(239,68,68,0.18)` | red |
| Info | `#1d4ed8` bg `rgba(59,130,246,0.1)` | `#93c5fd` bg `rgba(37,99,235,0.18)` | blue |

---

## 4. 背景与表面层

### 4.1 页面背景

```css
body[data-ui-mode='modern'] {
  background:
    radial-gradient(circle at top left, rgba(79, 107, 255, 0.09), transparent 24%),
    radial-gradient(circle at 100% 0, rgba(14, 165, 233, 0.08), transparent 20%),
    var(--ui-surface);
}
```

深色模式增加第三个径向渐变，形成更丰富的空间层次。

### 4.2 表面层叠（从底到顶）

| 层级 | 类名 | 透明度 | blur | 用途 |
|------|------|--------|------|------|
| L0 | `ui-modern-shell` | transparent | - | 外壳容器 |
| L1 | `ui-modern-sidebar` | 0.94 | - | 侧边栏 |
| L2 | `ui-modern-panel` | 0.86 | blur(18px) | 主内容面板 |
| L3 | `ui-modern-card` | 0.90 | - | 面板内卡片 |
| L4 | Modal / Dropdown | 0.95 | blur(20px) | 弹层 |

---

## 5. 布局体系

### 5.1 整体布局

```
┌──────────────────────────────────────────────────┐
│  ┌─────────┐  ┌──────────────────────────────┐   │
│  │         │  │  Page Header                 │   │
│  │ Sidebar │  │  ┌─────────────────────────┐ │   │
│  │         │  │  │ Toolbar                 │ │   │
│  │ ─ Nav   │  │  └─────────────────────────┘ │   │
│  │ ─ Nav   │  │                              │   │
│  │ ─ Nav   │  │  ┌──────┐ ┌──────┐ ┌──────┐ │   │
│  │ ─ Nav   │  │  │ Card │ │ Card │ │ Card │ │   │
│  │         │  │  └──────┘ └──────┘ └──────┘ │   │
│  │         │  │                              │   │
│  │         │  │  ┌─────────────────────────┐ │   │
│  │         │  │  │ Main Panel              │ │   │
│  │         │  │  │                         │ │   │
│  └─────────┘  │  └─────────────────────────┘ │   │
│               └──────────────────────────────┘   │
└──────────────────────────────────────────────────┘
```

### 5.2 布局规格

| 区域 | 宽度/尺寸 | 说明 |
|------|----------|------|
| 侧边栏 | 260px（桌面） / 抽屉式（移动） | 固定定位，支持折叠 |
| 内容区 | `calc(100% - sidebar)` | 自适应剩余空间 |
| 页面内边距 | 24px-32px | 响应式递减 |
| 卡片间距 | 16px-24px | grid gap |
| 最大内容宽度 | 1440px | 居中对齐 |

### 5.3 侧边栏

- 背景：`ui-modern-sidebar` 渐变
- 导航项：圆角 pill 形态（`rounded-xl`），选中态使用蓝色半透明渐变背景
- 分组：按功能模块分组，使用分隔线和小标题
- 底部：用户信息、设置、主题切换

```css
.ui-modern-nav-link.active {
  background: linear-gradient(135deg, rgba(59, 130, 246, 0.22), rgba(147, 197, 253, 0.12));
  color: #1d4ed8;
  box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.55), 0 10px 22px rgba(59, 130, 246, 0.12);
}
```

---

## 6. 组件规范

### 6.1 卡片（Card）

```css
.ui-modern-card {
  background: linear-gradient(145deg, rgba(255, 255, 255, 0.9), rgba(239, 246, 255, 0.7));
  border: 1px solid var(--ui-border);
  border-radius: 1.5rem;          /* rounded-3xl */
  box-shadow: 0 14px 34px rgba(15, 23, 42, 0.05);
}
```

变体：
- **统计卡片**：数字大字号 + 图标 + 变化趋势
- **内容卡片**：标题 + 正文 + 操作区
- **可交互卡片**：hover 时边框高亮 + 微上浮

### 6.2 面板（Panel）

```css
.ui-modern-panel {
  background: linear-gradient(145deg, rgba(255, 255, 255, 0.86), rgba(239, 246, 255, 0.64));
  border: 1px solid var(--ui-border);
  border-radius: 1.5rem;
  box-shadow: 0 18px 44px rgba(15, 23, 42, 0.06);
  backdrop-filter: blur(18px);
}
```

### 6.3 按钮

| 类型 | 类名 | 样式描述 |
|------|------|---------|
| 默认操作 | `page-modern-action` | 半透明白底，柔和边框，hover 增强 |
| 主要强调 | `page-modern-accent` | 蓝色渐变半透明背景，白色内阴影 |
| 主按钮 | `btn-primary` | `bg-violet-600` 实色（映射为 `#2563eb` 蓝色） |
| 次按钮 | `btn-secondary` | 灰色实色背景 |
| 成功 | `page-modern-success` | 绿色半透明渐变 |
| 警告 | `page-modern-warn` | 橙色半透明渐变 |
| 危险 | `page-modern-danger` | 红色半透明渐变 |

按钮统一特征：
- 圆角：`rounded-[0.95rem]`
- 内阴影：`inset 0 1px 0 rgba(255,255,255,0.5)`
- 过渡：`transition: all 160ms ease`
- 最小高度：`2.35rem`

### 6.4 输入框

```css
.page-modern input,
.page-modern select,
.page-modern textarea {
  border: 1px solid rgba(148, 163, 184, 0.28);
  background: rgba(255, 255, 255, 0.9);
  border-radius: 0.75rem;      /* rounded-lg */
  box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.55);
  padding: 0.5rem 0.75rem;
  font-size: 0.875rem;
}
```

Focus 态：`ring-2 ring-blue-500` + 边框加深

### 6.5 徽章（Badge）

```css
.badge { @apply inline-flex items-center px-2 py-0.5 rounded-full text-xs font-medium; }
.badge-green  { @apply bg-emerald-100 text-emerald-700; }
.badge-red    { @apply bg-red-100 text-red-700; }
.badge-blue   { @apply bg-blue-100 text-blue-700; }
.badge-yellow { @apply bg-amber-100 text-amber-700; }
```

Dark 模式使用 `dark:bg-xxx-900/30 dark:text-xxx-400` 变体。

### 6.6 工具栏（Toolbar）

```css
.page-modern-toolbar {
  display: flex;
  align-items: center;
  gap: 0.6rem;
  padding: 0.35rem;
  border: 1px solid rgba(191, 219, 254, 0.42);
  border-radius: 1.1rem;
  background: linear-gradient(145deg, rgba(255,255,255,0.76), rgba(239,246,255,0.64));
  backdrop-filter: blur(16px);
}
```

### 6.7 表格

| 元素 | 样式 |
|------|------|
| 表头 | 浅蓝灰色背景，bold 字重，sticky 定位 |
| 行 | hover 时半透明蓝色背景 |
| 边框 | 仅水平分隔线，无竖线 |
| 斑马纹 | 偶数行微灰背景（可选） |
| 圆角 | 表格容器 `rounded-2xl`，首尾行圆角继承 |

---

## 7. 动画与过渡

### 7.1 全局动效

```css
animation: {
  'fade-in':  'fadeIn 0.3s ease-out',
  'slide-up': 'slideUp 0.3s ease-out',
  'pulse-slow': 'pulse 3s cubic-bezier(0.4, 0, 0.6, 1) infinite',
}
```

### 7.2 交互微动效

| 场景 | 动效 | 时长 |
|------|------|------|
| 页面进入 | fadeIn + slideUp | 300ms |
| 卡片 hover | translate-Y(-2px) + shadow 增强 | 160ms |
| 按钮点击 | scale(0.98) | 100ms |
| 弹窗打开 | fadeIn + scale(0.95 → 1) | 200ms |
| 状态指示 | pulse（加载中、在线状态） | 持续 |
| 侧边栏折叠 | width 过渡 + 内容淡出 | 250ms |

### 7.3 性能降级

提供 `data-ui-perf="reduced"` 属性，自动关闭 blur 和复杂阴影：

```css
.ui-modern-shell[data-ui-perf='reduced'] [class*='backdrop-blur'] {
  backdrop-filter: none !important;
}
```

---

## 8. 排版规范

### 8.1 字体栈

```css
font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'PingFang SC', 'Microsoft YaHei', sans-serif;
```

### 8.2 字号体系

| 层级 | 字号 | 字重 | 行高 | 用途 |
|------|------|------|------|------|
| 页面标题 | 1.65rem | 800 | 1.1 | 页面主标题 |
| 副标题 | 0.92rem | 400 | 1.5 | 页面描述 |
| 区块标题 | 1.125rem (text-lg) | 700 | 1.3 | 卡片/面板标题 |
| 正文 | 0.875rem (text-sm) | 400 | 1.6 | 默认正文 |
| 辅助文字 | 0.8rem (text-xs) | 400 | 1.5 | 说明、时间戳 |
| 徽章/标签 | 0.75rem (text-xs) | 500 | 1 | Badge、Tag |

### 8.3 标题样式

```css
.page-modern-title {
  font-size: 1.65rem;
  font-weight: 800;
  letter-spacing: -0.03em;
  color: #0f172a;    /* Light */
  /* Dark: #f8fbff */
}
```

---

## 9. 响应式断点

| 断点 | 宽度 | 布局变化 |
|------|------|---------|
| Mobile | < 768px | 侧边栏隐藏为抽屉，卡片单列，标题字号 1.2rem |
| Tablet | 768-1023px | 侧边栏可折叠，卡片双列，工具栏换行 |
| Desktop | ≥ 1024px | 侧边栏固定展示，完整布局 |

移动端额外规则：
- 输入框 `font-size: 16px`（防止 iOS 缩放）
- 卡片圆角收缩至 `rounded-xl`
- 工具栏宽度 100%，flex-wrap

---

## 10. 暗色模式实现

### 10.1 切换机制

```html
<html class="dark">
```

通过 `darkMode: 'class'` 配置，配合 `localStorage` 持久化用户偏好。

### 10.2 深色主题特征

- 背景：深海军蓝（`#07111f`），多层径向渐变增加空间感
- 面板：低透明度深蓝（`rgba(12, 24, 42, 0.58)`），保留毛玻璃质感
- 文字：冷白偏蓝（`#e8f1ff`）
- 边框：蓝色调微光（`rgba(125, 166, 255, 0.14)`）
- 强调色：亮度提升（`#60a5fa`）
- 内阴影：`inset 0 1px 0 rgba(255, 255, 255, 0.04)` 模拟顶部光泽

---

## 11. AInnoStar 专属页面设计

### 11.1 全局看板（Dashboard）

```
┌─────────────────────────────────────────────────────┐
│  全局看板                        [筛选] [时间范围]   │
├─────────┬──────────┬──────────┬─────────────────────┤
│ 调研洞察 │ 需求池   │ 进行中   │ Sprint 完成率       │
│  128    │  56      │  12      │  78%                │
│ ↑12%    │ ↑8       │ →        │ ↑5%                 │
├─────────┴──────────┴──────────┴─────────────────────┤
│                                                     │
│  ┌─────────────────────┐  ┌───────────────────────┐ │
│  │ 需求漏斗             │  │ 产品健康度             │ │
│  │ 洞察→需求→PRD→上线   │  │ NPS / 反馈率 / 缺陷率  │ │
│  │     [漏斗图]         │  │     [仪表盘]           │ │
│  └─────────────────────┘  └───────────────────────┘ │
│                                                     │
│  ┌─────────────────────────────────────────────────┐│
│  │ 迭代时间线                                       ││
│  │ v1.0 ─────── v1.1 ─────── v1.2 ──── Now        ││
│  └─────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────┘
```

### 11.2 调研模块

```
┌─────────────────────────────────────────────────────┐
│  用户调研                         [+ 新建调研项目]   │
│  管理访谈、分析洞察、沉淀用户画像                     │
├─────────────────────────────────────────────────────┤
│  [调研列表] [洞察看板] [用户画像] [受访者库]          │
├─────────────────────────────────────────────────────┤
│                                                     │
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐│
│  │ 项目A         │ │ 项目B         │ │ 项目C         ││
│  │ 12次访谈      │ │ 8次访谈       │ │ 进行中        ││
│  │ ● 已完成      │ │ ● 进行中      │ │ ● 计划中      ││
│  │ 32个洞察标签  │ │ 18个洞察标签  │ │ -             ││
│  └──────────────┘ └──────────────┘ └──────────────┘│
│                                                     │
│  ┌─────────────────────────────────────────────────┐│
│  │ 访谈记录                  [AI 转录] [导出报告]   ││
│  │ ┌──────────────────────────────────────────────┐││
│  │ │ Q: 您在使用搜索功能时遇到哪些问题？           │││
│  │ │ A: 搜索结果排序不太合理，经常找不到想要的…   │││
│  │ │ 🔴 痛点: 搜索体验  📌 标签: 搜索优化         │││
│  │ └──────────────────────────────────────────────┘││
│  └─────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────┘
```

### 11.3 文档模块（PRD 编辑器）

```
┌─────────────────────────────────────────────────────┐
│  产品文档            [需求池] [PRD列表] [竞品分析]    │
├──────────┬──────────────────────────────────────────┤
│          │  PRD: 用户搜索功能优化 V2.0               │
│ 需求池   │  ┌────────────────────────────────────┐   │
│          │  │ 背景与目标                          │   │
│ ○ 搜索优化│  │ 基于调研反馈，60%用户认为当前搜索… │   │
│ ○ 导航改进│  │                                    │   │
│ ○ 性能提升│  │ ## 功能清单                        │   │
│ ○ 新增筛选│  │ 1. 搜索结果智能排序                │   │
│          │  │ 2. 搜索建议与自动补全              │   │
│ 已选入PRD│  │ 3. 高级筛选面板                    │   │
│ ● 搜索优化│  │                                    │   │
│ ● 导航改进│  │ [AI 生成初稿] [邀请评审] [导出]    │   │
│          │  └────────────────────────────────────┘   │
│ [AI排序] │                                           │
│          │  版本历史: V1.0 → V1.1(评审通过) → V2.0   │
└──────────┴──────────────────────────────────────────┘
```

### 11.4 原型生成模块

```
┌─────────────────────────────────────────────────────┐
│  AI 原型生成                     [选择生成路径 ▾]     │
│  基于 PRD 自动生成可交互原型                          │
├─────────────────────────────────────────────────────┤
│                                                     │
│  信息架构图                    生成路径               │
│  ┌───────────────────┐       ┌────────────────────┐ │
│  │ Sitemap           │       │ ⚡ 多Agent生成 [P0] │ │
│  │ ├─ 首页           │       │ 🔌 MCP/Skill  [P0] │ │
│  │ ├─ 搜索           │       │ 📁 挂载本地   [P0] │ │
│  │ │  ├─ 结果页       │       └────────────────────┘ │
│  │ │  └─ 详情页       │                             │
│  │ └─ 设置           │       生成进度                │
│  └───────────────────┘       ┌────────────────────┐ │
│                              │ ✅ 架构Agent 完成   │ │
│  交互流程图                   │ ✅ 布局Agent 完成   │ │
│  ┌───────────────────┐       │ 🔄 组件Agent 进行中 │ │
│  │ [Flow Diagram]    │       │ ⏳ 样式Agent 等待   │ │
│  └───────────────────┘       └────────────────────┘ │
│                                                     │
│  ┌─────────────────────────────────────────────────┐│
│  │ 原型预览        [全屏] [分享链接] [导出代码]     ││
│  │ ┌───────────────────────────────────────────┐   ││
│  │ │                                           │   ││
│  │ │         [ 页面实时预览沙箱 ]                │   ││
│  │ │                                           │   ││
│  │ └───────────────────────────────────────────┘   ││
│  │ [自然语言修改: 输入修改指令...]                   ││
│  └─────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────┘
```

### 11.5 开发跟踪模块

```
┌─────────────────────────────────────────────────────┐
│  开发跟踪                [技术路线] [Sprint] [缺陷]   │
├─────────────────────────────────────────────────────┤
│                                                     │
│  Sprint 12: 搜索功能优化        2026/04/15 - 04/28  │
│  ┌──────────┬──────────┬──────────┬──────────┐      │
│  │ 待办     │ 进行中   │ 测试中   │ 已完成   │      │
│  │          │          │          │          │      │
│  │ ┌──────┐ │ ┌──────┐ │ ┌──────┐ │ ┌──────┐│      │
│  │ │API开发│ │ │前端联调│ │ │排序测试│ │ │UI设计││      │
│  │ │@张三 │ │ │@李四 │ │ │@王五 │ │ │@赵六 ││      │
│  │ │P1 🔴 │ │ │P0 🔴 │ │ │P1 🟡 │ │ │P0 🟢 ││      │
│  │ └──────┘ │ └──────┘ │ └──────┘ │ └──────┘│      │
│  │ ┌──────┐ │          │          │ ┌──────┐│      │
│  │ │DB设计│ │          │          │ │PRD评审││      │
│  │ │@钱七 │ │          │          │ │@PM   ││      │
│  │ └──────┘ │          │          │ └──────┘│      │
│  └──────────┴──────────┴──────────┴──────────┘      │
│                                                     │
│  ┌─────────────────────────────────────────────────┐│
│  │ 燃尽图              [Burndown Chart]             ││
│  │ ╲                                               ││
│  │  ╲  ····                                        ││
│  │   ╲·     ····                                   ││
│  │    ·          ·····───                           ││
│  └─────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────┘
```

### 11.6 反馈模块

```
┌─────────────────────────────────────────────────────┐
│  用户反馈                [收集] [分析] [闭环]         │
├─────────────────────────────────────────────────────┤
│                                                     │
│  反馈概览                                           │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌────────┐ │
│  │ 总反馈    │ │ 待处理    │ │ 已闭环    │ │ NPS    │ │
│  │ 1,234    │ │ 89       │ │ 1,056    │ │ 42     │ │
│  │ ↑15%     │ │ ↓8%      │ │ ↑12%     │ │ ↑3     │ │
│  └──────────┘ └──────────┘ └──────────┘ └────────┘ │
│                                                     │
│  AI 聚类分析                                        │
│  ┌─────────────────────────────────────────────────┐│
│  │ Top 1: 搜索体验差 (50条)  情感: 😡 负面 82%     ││
│  │ Top 2: 加载速度慢 (35条)  情感: 😐 中性 60%     ││
│  │ Top 3: 界面美观 (28条)   情感: 😊 正面 90%     ││
│  └─────────────────────────────────────────────────┘│
│                                                     │
│  反馈列表                                           │
│  ┌─────────────────────────────────────────────────┐│
│  │ "搜索结果总是找不到" ─ 🏷 Bug ─ 🔴 负面         ││
│  │ → AI建议: 关联需求 REQ-023「搜索优化」          ││
│  │   [确认关联] [忽略]                              ││
│  └─────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────┘
```

---

## 12. 导航结构

### 12.1 侧边栏导航

```
Logo / 品牌
─────────────
📊 全局看板
─────────────
🔍 用户调研
  ├─ 调研项目
  ├─ 洞察看板
  └─ 用户画像
─────────────
📝 产品文档
  ├─ 需求池
  ├─ PRD 管理
  └─ 竞品分析
─────────────
🎨 AI 原型
  ├─ 生成工作台
  ├─ 信息架构
  └─ 原型库
─────────────
🚀 开发跟踪
  ├─ 技术路线
  ├─ Sprint 看板
  ├─ 缺陷管理
  └─ 发布管理
─────────────
💬 用户反馈
  ├─ 反馈收集
  ├─ 分析看板
  └─ 闭环管理
─────────────
⚙️ 设置
  ├─ 团队管理
  ├─ AI 配置
  └─ 集成管理
```

### 12.2 面包屑

页面顶部显示层级路径：`全局看板 > 用户调研 > 调研项目A`

---

## 13. AI 交互模式

### 13.1 AI 状态指示

| 状态 | 视觉表现 |
|------|---------|
| 空闲 | 无特殊指示 |
| 生成中 | 蓝色脉冲点 + 渐进式进度条 |
| 完成 | 绿色对勾 + 结果面板滑入 |
| 失败 | 红色警示 + 重试按钮 |

### 13.2 AI 操作面板

AI 功能入口统一使用 `page-modern-accent` 按钮样式（蓝色渐变），与普通操作区分。

典型 AI 交互流程：
1. 用户点击 AI 按钮
2. 弹出确认/参数面板
3. 显示生成进度（流式输出）
4. 结果预览 + 用户确认/修改
5. 采纳或重新生成

---

## 14. 图标规范

使用 **Lucide React** 图标库，统一风格：

| 模块 | 图标 | 说明 |
|------|------|------|
| 全局看板 | `LayoutDashboard` | 仪表盘 |
| 用户调研 | `Search` / `Users` | 搜索/用户 |
| 产品文档 | `FileText` / `BookOpen` | 文档/书 |
| AI 原型 | `Sparkles` / `Palette` | AI/设计 |
| 开发跟踪 | `GitBranch` / `CheckSquare` | 分支/任务 |
| 用户反馈 | `MessageSquare` / `ThumbsUp` | 消息/反馈 |
| 设置 | `Settings` | 齿轮 |

图标尺寸：导航 20px，按钮内 16px，装饰 24px

---

## 15. 数据可视化风格

### 15.1 图表色彩

与整体色彩体系统一，使用蓝色系为主，辅以语义色：

```
主色: #4f6bff → #60a5fa（蓝色渐变）
辅色: #10b981（成功/正面）
      #f59e0b（警告/中性）
      #ef4444（危险/负面）
      #8b5cf6（强调/特殊）
```

### 15.2 图表样式

- 背景透明或极淡
- 网格线：`rgba(148, 163, 184, 0.15)`
- 数值标签：`text-sm font-medium`
- Tooltip：毛玻璃风格面板
- 动画：数据加载时从 0 渐增

---

## 16. 空状态与加载态

### 16.1 空状态

- 居中展示插图/图标 + 说明文字 + 操作按钮
- 图标使用 `lucide-react` 大尺寸 + 蓝色半透明背景
- 文案示例：「还没有调研项目，创建第一个开始吧」

### 16.2 加载态

```tsx
<div className="inline-flex items-center gap-3 rounded-2xl border border-slate-200/70 bg-white/90 px-4 py-3 text-sm text-slate-600 shadow-sm">
  <span className="h-2.5 w-2.5 animate-pulse rounded-full bg-blue-500" />
  <span>加载中...</span>
</div>
```

---

## 17. 性能优化

| 策略 | 实现方式 |
|------|---------|
| 路由懒加载 | `React.lazy()` + `Suspense` |
| 组件 Memo | `memo()` 包裹纯展示组件 |
| 减少重渲染 | `useMemo` / `useCallback` |
| 图片懒加载 | `loading="lazy"` |
| CSS 降级 | `data-ui-perf="reduced"` 关闭 blur/animation |
| 代码分割 | 按页面模块分割 chunk |

---

## 附录: 设计 Token 快速参考

```css
/* 表面 */
.bg-modern-surface    { background: var(--ui-surface); }
.bg-modern-panel      { background: var(--ui-panel); backdrop-filter: blur(18px); }
.bg-modern-card       { background: linear-gradient(145deg, rgba(255,255,255,0.9), rgba(239,246,255,0.7)); }

/* 边框 */
.border-modern        { border: 1px solid var(--ui-border); }

/* 文字 */
.text-modern          { color: var(--ui-text); }
.text-modern-muted    { color: var(--ui-muted); }

/* 强调 */
.text-modern-accent   { color: var(--ui-accent); }
.bg-modern-accent     { background: var(--ui-accent-soft); }

/* 阴影 */
.shadow-modern        { box-shadow: 0 14px 34px rgba(15,23,42,0.05); }
.shadow-modern-lg     { box-shadow: 0 20px 44px rgba(15,23,42,0.06); }
```
